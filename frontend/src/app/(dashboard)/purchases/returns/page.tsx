import ComingSoon from "@/components/shared/ComingSoon";

export default function PurchaseReturnsPage() {
  return (
    <ComingSoon
      title="Purchase Returns"
      description="Return goods to suppliers and reverse the payable. The tables already exist (purchase_returns, purchase_return_items); the screens are next on the roadmap. Meanwhile, tell the AI: &quot;Return the laptop to ABC Computers&quot; and it will prepare the reversal."
    />
  );
}
