# =============================================================================
# ERP AI Agent — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Python >= 3.11 required
# =============================================================================

# ---- Web Framework ----
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9

# ---- Supabase ----
supabase>=2.7.0,<3.0

# ---- Google Gemini ----
google-genai>=1.0.0,<3.0

# ---- Data Validation ----
pydantic>=2.9.0,<3.0
pydantic-settings>=2.5.0,<3.0

# ---- Auth / Security ----
python-jose[cryptography]>=3.3.0,<4.0
passlib[bcrypt]>=1.7.4,<2.0
PyJWT>=2.8.0,<3.0                # JWT decode/verify (ES256 via JWKS + HS256 legacy)

# ---- Utilities ----
httpx>=0.27.0,<1.0
tenacity>=9.0.0,<10.0          # Retry logic for API calls
structlog>=24.4.0,<25.0        # Structured logging

# ---- Testing ----
pytest>=8.3.0,<9.0
pytest-asyncio>=0.24.0,<1.0
pytest-cov>=5.0.0,<6.0
